// mobile-fit node half: no host-side behavior. This package exists only to
// ship a browser client bundle (the mobile UI adaptation layer).
// Mirrors @deepseek-ai/dsh-client-ui-layout's host entry shape.
export function apply() {}
